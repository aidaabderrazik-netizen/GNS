Hello,
le script routeurs.py a pour rôle de créer le fichier json / le remplir en appelant la fonction generate_routers.
le script gns-deployment est pour créer les liens rip, ospf ou ebgp entre les routeurs. faut juste que je les teste sur gns pour voir si ça marche vraiment.
il nous reste à faire après cela les policies de bgp (filtering,...)
